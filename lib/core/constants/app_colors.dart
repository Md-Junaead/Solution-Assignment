import 'package:flutter/material.dart';

/// Central place for all app colors
/// Ensures consistency across the app
class AppColors {
  // Grok primary blue (used for button, accents)
  static const Color grokBlue = Color(0xFF4285F4);

  // Underline color for grammar/wordy issues (light blue)
  static const Color issueUnderline = Color(0xFF8AB4F8);

  // Background colors
  static const Color scaffoldBackground = Color(0xFFF8F9FA);
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color primaryText = Color(0xFF202124);
  static const Color secondaryText = Color(0xFF5F6368);
}
